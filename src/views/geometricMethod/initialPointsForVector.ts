import { tPointsForVector } from '@/views/geometricMethod/types';

export default (function (): tPointsForVector {
  return {
    lines: [
      { x: null, y: null },
      { x: null, y: null },
    ],
  };
})();

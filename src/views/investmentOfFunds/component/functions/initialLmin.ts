import { tLmin } from '@/views/investmentOfFunds/component';

function initialLmin(): tLmin {
  return {
    coefficients: [{ x: null }, { x: null }, { x: null }, { x: null }],
    result: null,
  };
}

export { initialLmin };
